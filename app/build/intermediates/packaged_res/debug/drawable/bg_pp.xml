<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Latar belakang berwarna putih -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/green_primary" /> <!-- Warna latar belakang -->
            <corners android:radius="1000dp" /> <!-- Radius batas (sesuaikan sesuai kebutuhan) -->
            <stroke android:width="4dp" android:color="@color/white"/>
        </shape>
    </item>
</layer-list>